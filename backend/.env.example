# Backend environment
PORT=4000
JWT_SECRET=dev
CLIENT_URL=http://localhost:19006
# Postgres connection (enable to use PG instead of JSON db)
DATABASE_URL=postgresql://faceEnhance:faceEnhancer925@enhance.ai@localhost:5433/faceenhancerdatabase
# Set to true on managed hosts that require SSL
PGSSL=false
